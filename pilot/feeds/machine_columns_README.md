# Reservedels-feed kolonnemap

Reservedels-feeds har to ekstra kolonner ud over standardproduktet:
`machine_brand` + `machine_model` + `oem_no` → matcher reservedelen til maskine/OEM-nummer.
Disse mappes til `compatibility`-feltet på produktet.
