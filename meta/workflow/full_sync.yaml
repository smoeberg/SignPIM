name: FullSyncWorkflow
steps:
  - action: transform
    rules: [{ field: price, op: multiply, value: 1.25 }, { field: name, op: uppercase }]
  - action: validate
    rules: [missing_images, ready_for_publication]
  - action: calculate_quality
  - action: auto_resolve
  - action: persist
